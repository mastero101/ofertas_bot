<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        .email-container {
            max-width: 600px;
            margin: 0 auto;
            font-family: Arial, sans-serif;
        }
        .header {
            background-color: #2c3e50;
            color: white;
            padding: 20px;
            display: flex;
            align-items: center;
            gap: 20px;
        }
        .header img {
            max-width: 200px;
            height: auto;
            margin: 0;
        }
        .header h1 {
            margin: 0;
            font-size: 32px;
            flex-grow: 1;
            text-align: left;
        }
        .header img {
            max-width: 150px;
            height: auto;
            margin: 0;
        }
        .content {
            padding: 20px;
            background-color: #ffffff;
        }
        .offer {
            background-color: #f8f9fa;
            border: 1px solid #dee2e6;
            padding: 15px;
            margin: 10px 0;
            border-radius: 5px;
        }
        .button {
            background-color: #e74c3c;
            color: white;
            padding: 10px 20px;
            text-decoration: none;
            border-radius: 5px;
            display: inline-block;
            margin: 10px 0;
        }
        .footer {
            background-color: #34495e;
            color: white;
            padding: 15px;
            text-align: center;
            font-size: 12px;
        }
    </style>
</head>
<body>
    <div class="email-container">
        <div class="header">
            <img src="https://i.ibb.co/qMyDkKwv/HAMSE-Logo.png" alt="" style="max-width: 200px; height: auto; margin-bottom: 15px;">
            <!--
            <h1><%= companyName %></h1>
            -->
        </div>
        <div class="content">
            <h2>¡Ofertas Especiales!</h2>
            <p>Estimado/a <%= customerName %>,</p>
            <p>Tenemos algunas ofertas increíbles para ti:</p>
            
            <div class="offer">
                <h3><%= offerTitle %></h3>
                <% if (productImage) { %>
                    <div class="product-image">
                        <img src="<%= productImage %>" alt="<%= offerTitle %>" style="max-width: 100%; height: auto; margin: 15px 0; border-radius: 8px;">
                    </div>
                <% } %>
                <p><%= offerDescription %></p>
                <% if (offerPrice) { %>
                    <p style="font-size: 1.2em; color: #e74c3c;">Precio Especial: $<%= offerPrice %></p>
                <% } %>
                <a href="<%= trackingLink(offerLink) %>" class="button">Ver Oferta</a>
            </div>
            
            <p>¡No te pierdas estas increíbles ofertas!</p>
        </div>
        <div class="footer">
            <p>© <%= new Date().getFullYear() %> <%= companyName %>. Todos los derechos reservados.</p>
            <p>Si deseas cancelar tu suscripción, <a href="<%= trackingLink(unsubscribeLink) %>" style="color: white;">haz clic aquí</a></p>
        </div>
    </div>
    <!-- Pixel de seguimiento -->
    <img src="<%= trackingPixel %>" width="1" height="1" alt="" style="display:none">
</body>
</html>